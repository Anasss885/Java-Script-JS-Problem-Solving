function numberToString(num) {
  // Return a string of the number here!
  return num.toString()
}
//Another Sol

function numberToString(num) {
 
  return `${num}`;  /// backtick is an operator that allow us to deal with strings in a differnt way , by inducing variabels that holds string value in the logic 
}
